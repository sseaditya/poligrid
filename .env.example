# Copy this file to .env.local and fill keys.
OPENAI_API_KEY=
GEMINI_API_KEY=
PORT=8080

# Supabase (get from Project Settings → API)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=   # server-only, never expose to client
SUPABASE_ANON_KEY=           # safe to expose to client (used for Auth)
